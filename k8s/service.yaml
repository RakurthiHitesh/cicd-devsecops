apiVersion: v1
kind: Service
metadata:
  name: flask-devsecops-svc
  namespace: devsecops-app
  labels:
    app: flask-devsecops-app
spec:
  type: LoadBalancer
  selector:
    app: flask-devsecops-app
  ports:
  - protocol: TCP
    port: 80
    targetPort: 5000
